# 🚀 HomeVerse 部署指南

## 📋 部署准备

### 服务器要求

- **操作系统**: Ubuntu 20.04+ / CentOS 7+ / Debian 10+
- **内存**: 最低 2GB，推荐 4GB+
- **磁盘**: 最低 20GB，推荐 50GB+
- **Node.js**: 16.x 或更高
- **MongoDB**: 4.4 或更高
- **Nginx**: 1.18 或更高

---

## 🔧 部署步骤

### 1. 安装依赖

#### 安装 Node.js

```bash
# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 验证安装
node -v
npm -v
```

#### 安装 MongoDB

```bash
# Ubuntu/Debian
wget -qO - https://www.mongodb.org/static/pgp/server-6.0.asc | sudo apt-key add -
echo "deb [ arch=amd64,arm64 ] https://repo.mongodb.org/apt/ubuntu focal/mongodb-org/6.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-6.0.list
sudo apt-get update
sudo apt-get install -y mongodb-org

# 启动 MongoDB
sudo systemctl start mongod
sudo systemctl enable mongod
```

#### 安装 Nginx

```bash
# Ubuntu/Debian
sudo apt-get install -y nginx

# 启动 Nginx
sudo systemctl start nginx
sudo systemctl enable nginx
```

---

### 2. 部署后端

#### 上传代码

```bash
# 创建项目目录
sudo mkdir -p /var/www/homeverse
cd /var/www/homeverse

# 上传代码（使用 git 或 scp）
git clone <你的仓库地址> .
# 或
scp -r /本地路径/HomeVerse/* user@server:/var/www/homeverse/
```

#### 安装依赖

```bash
cd /var/www/homeverse/backend
npm install --production
```

#### 配置环境变量

```bash
# 创建生产环境配置
cp .env.example .env
nano .env
```

**配置内容**：
```env
# 数据库
MONGODB_URI=mongodb://localhost:27017/homeverse

# JWT密钥（必须修改为强密钥）
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# 密码加密密钥（32字符）
ENCRYPTION_KEY=your-32-character-encryption-key-change-this!!

# 邮件服务
MAIL_HOST=smtp.qq.com
MAIL_PORT=465
MAIL_SECURE=true
MAIL_USER=your-email@qq.com
MAIL_PASSWORD=your-authorization-code

# 前端URL
FRONTEND_URL=https://homeverse.yourdomain.com

# 后端端口
PORT=3001
NODE_ENV=production
```

#### 构建后端

```bash
npm run build
```

#### 使用 PM2 管理进程

```bash
# 安装 PM2
sudo npm install -g pm2

# 启动后端服务
pm2 start npm --name "homeverse-backend" -- run start:prod

# 设置开机自启
pm2 startup
pm2 save

# 查看状态
pm2 status
pm2 logs homeverse-backend
```

---

### 3. 部署前端

#### 构建前端

```bash
cd /var/www/homeverse/frontend

# 安装依赖
npm install

# 构建生产版本
npm run build
```

**构建输出**：`dist/` 目录

#### 配置前端环境（可选）

如果需要修改API地址，编辑 `vite.config.ts`：

```typescript
export default defineConfig({
  // ...
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:3001',  // 修改为实际后端地址
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, ''),
      },
    },
  },
});
```

---

### 4. 配置 Nginx

#### 复制配置文件

```bash
# 复制 nginx.conf 到 Nginx 配置目录
sudo cp /var/www/homeverse/nginx.conf /etc/nginx/sites-available/homeverse

# 创建软链接
sudo ln -s /etc/nginx/sites-available/homeverse /etc/nginx/sites-enabled/

# 删除默认配置（可选）
sudo rm /etc/nginx/sites-enabled/default
```

#### 修改配置

```bash
sudo nano /etc/nginx/sites-available/homeverse
```

**必须修改的地方**：
```nginx
# 1. 域名
server_name homeverse.yourdomain.com;  # 改为你的域名

# 2. 前端路径
root /var/www/homeverse/frontend/dist;  # 确认路径正确

# 3. 上传文件路径
location /uploads/ {
    alias /var/www/homeverse/backend/uploads/;  # 确认路径正确
}
```

#### 测试配置

```bash
# 测试 Nginx 配置
sudo nginx -t

# 如果显示 "syntax is ok" 和 "test is successful"
# 重新加载配置
sudo systemctl reload nginx
```

---

### 5. 配置 SSL（HTTPS）

#### 使用 Let's Encrypt 免费证书

```bash
# 安装 Certbot
sudo apt-get install -y certbot python3-certbot-nginx

# 获取证书（自动配置 Nginx）
sudo certbot --nginx -d homeverse.yourdomain.com

# 自动续期
sudo certbot renew --dry-run
```

#### 手动配置 SSL

如果已有证书：

```bash
# 上传证书文件
sudo mkdir -p /etc/nginx/ssl
sudo cp homeverse.crt /etc/nginx/ssl/
sudo cp homeverse.key /etc/nginx/ssl/

# 修改 nginx.conf 中的证书路径
ssl_certificate /etc/nginx/ssl/homeverse.crt;
ssl_certificate_key /etc/nginx/ssl/homeverse.key;

# 重新加载
sudo nginx -t
sudo systemctl reload nginx
```

---

### 6. 配置防火墙

```bash
# UFW (Ubuntu)
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable

# 或 firewalld (CentOS)
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

---

## 📊 目录结构

```
/var/www/homeverse/
├── backend/
│   ├── dist/              # 编译后的代码
│   ├── uploads/           # 上传的文件
│   ├── node_modules/
│   ├── .env              # 环境变量（重要）
│   └── package.json
├── frontend/
│   └── dist/             # 构建后的静态文件
│       ├── index.html
│       ├── assets/
│       └── images/
└── nginx.conf            # Nginx 配置
```

---

## 🔍 验证部署

### 1. 检查后端

```bash
# 查看 PM2 状态
pm2 status

# 查看日志
pm2 logs homeverse-backend

# 测试后端 API
curl http://localhost:3001/health-check
```

### 2. 检查前端

```bash
# 访问域名
curl http://homeverse.yourdomain.com

# 应该返回 HTML
```

### 3. 检查 Nginx

```bash
# 检查 Nginx 状态
sudo systemctl status nginx

# 查看错误日志
sudo tail -f /var/log/nginx/homeverse_error.log
```

---

## 🔧 常见问题

### 问题1：502 Bad Gateway

**原因**：后端服务未启动或端口不对

**解决**：
```bash
pm2 status
pm2 restart homeverse-backend
```

### 问题2：404 Not Found

**原因**：前端文件路径不对

**解决**：
```bash
# 检查前端文件是否存在
ls -la /var/www/homeverse/frontend/dist/

# 检查 Nginx 配置中的 root 路径
```

### 问题3：文件上传失败

**原因**：文件大小限制

**解决**：
```nginx
# 在 nginx.conf 中增加
client_max_body_size 100M;  # 调整大小
```

### 问题4：CORS 错误

**原因**：跨域配置问题

**解决**：后端已配置 CORS，确保前端通过 Nginx 代理访问

---

## 📝 维护命令

### PM2 管理

```bash
# 查看状态
pm2 status

# 重启服务
pm2 restart homeverse-backend

# 停止服务
pm2 stop homeverse-backend

# 查看日志
pm2 logs homeverse-backend

# 清空日志
pm2 flush
```

### Nginx 管理

```bash
# 测试配置
sudo nginx -t

# 重新加载配置
sudo systemctl reload nginx

# 重启 Nginx
sudo systemctl restart nginx

# 查看状态
sudo systemctl status nginx
```

### 更新部署

```bash
# 1. 拉取最新代码
cd /var/www/homeverse
git pull

# 2. 更新后端
cd backend
npm install
npm run build
pm2 restart homeverse-backend

# 3. 更新前端
cd ../frontend
npm install
npm run build

# 4. 重新加载 Nginx
sudo systemctl reload nginx
```

---

## 🔐 安全建议

### 1. 修改默认密钥

```env
# .env 中必须修改
JWT_SECRET=使用强随机密钥
ENCRYPTION_KEY=使用32字符强随机密钥
```

### 2. 配置 HTTPS

生产环境必须使用 HTTPS

### 3. 防火墙配置

只开放必要端口（80, 443）

### 4. 定期备份

```bash
# MongoDB 备份
mongodump --db homeverse --out /backup/mongodb/$(date +%Y%m%d)

# 文件备份
tar -czf /backup/uploads_$(date +%Y%m%d).tar.gz /var/www/homeverse/backend/uploads/
```

### 5. 日志轮转

```bash
# 配置日志轮转
sudo nano /etc/logrotate.d/homeverse

# 内容：
/var/log/nginx/homeverse_*.log {
    daily
    missingok
    rotate 14
    compress
    delaycompress
    notifempty
    create 0640 www-data adm
    sharedscripts
    postrotate
        [ -f /var/run/nginx.pid ] && kill -USR1 `cat /var/run/nginx.pid`
    endscript
}
```

---

## 📊 性能优化

### 1. 开启 Gzip

已在 nginx.conf 中配置

### 2. 静态资源缓存

已配置：
- JS/CSS/图片：1年
- HTML：不缓存

### 3. MongoDB 索引

```bash
mongo homeverse

# 创建常用字段索引
db.calendar_events.createIndex({ familyId: 1, startDate: 1 })
db.transactions.createIndex({ familyId: 1, date: -1 })
db.articles.createIndex({ familyId: 1, createdAt: -1 })
```

---

## 🎉 部署完成检查

- [ ] Node.js 已安装
- [ ] MongoDB 已安装并运行
- [ ] Nginx 已安装并运行
- [ ] 代码已上传
- [ ] .env 已配置
- [ ] 后端已构建
- [ ] PM2 已启动后端
- [ ] 前端已构建
- [ ] Nginx 配置已生效
- [ ] 可以访问域名
- [ ] HTTPS 已配置（生产环境）
- [ ] 所有功能测试通过

---

## 🌐 访问地址

**HTTP**: http://homeverse.yourdomain.com  
**HTTPS**: https://homeverse.yourdomain.com  

---

**部署完成后，HomeVerse 即可在生产环境使用！** 🚀✨

